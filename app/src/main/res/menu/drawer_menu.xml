<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    >
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/ic_message"
            android:icon="@drawable/ic_message"
            android:title="Message"></item>
        <item
            android:id="@+id/ic_profile"
            android:icon="@drawable/ic_profile"
            android:title="Your bill"></item>
        <item
            android:id="@+id/ic_setting"
            android:icon="@drawable/ic_setting"
            android:title="Setting"></item>
        <item
            android:visible="false"
            android:id="@+id/ic_bill"
            android:icon="@drawable/ic_setting"
            android:title="List bill"></item>
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:visible="false"
                android:id="@+id/ic_share"
                android:icon="@drawable/ic_share_black_24dp"
                android:title="Share"></item>
            <item
                android:id="@+id/ic_account"
                android:icon="@drawable/ic_send"
                android:title="Log in"></item>
            <item
                android:visible="false"
                android:id="@+id/ic_file_check"
                android:icon="@drawable/ic_send"
                android:title="File Test"></item>
            <item
                android:id="@+id/ic_log_out"
                android:icon="@drawable/ic_send"
                android:title="Log out"></item>
        </menu>
    </item>
</menu>